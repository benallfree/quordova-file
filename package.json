{
  "name": "qordova-file",
  "version": "1.0.0",
  "description": "Q-enabled File interface for Cordova",
  "main": "./build/file",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ben Allfree",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^12.0.1",
    "coffeeify": "^2.0.1"
  },
  "scripts": {
    "build":  "browserify -t coffeeify --extension='.coffee' src/file.coffee -o build/file.js"
  }
}
